# This is where env vars come from

# this is the important bit
spring.profiles.active=dev


# create an application-dev.properties file and paste this in to that file
# uncommenting and filling in the relevant details
#spring.datasource.url=jdbc:postgresql://<server address>:5432/<db name>
#spring.datasource.username=<db username>
#spring.datasource.password=<db password>
#spring.jpa.show-sql=true
#
### Hibernate Properties
## The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect
#
## Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = update
